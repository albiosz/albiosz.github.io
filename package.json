{
  "name": "one-page-resume",
  "version": "1.0.0",
  "main": "data.js",
  "scripts": {
    "dev": "npm run scss & npm run auto-reload && fg",
    "dev-op": "npm run scss-op & npm run auto-reload && fg",

    "auto-reload": "browser-sync start --server --dierctory --files \"**/*\"",
    
    "scss": "sass --watch style.scss style.css & sass --watch print.scss print.css && fg",
    "scss-op": "sass --watch one-page-resume/style.scss one-page-resume/style.css"
  },
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "sass": "^1.77.2"
  }
}
